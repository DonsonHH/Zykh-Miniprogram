# 安全说明

## 支持范围

此仓库维护微信小程序与配套 CloudBase API。Station、设备固件、实体药箱与生产数据库的安全问题应在对应仓库和部署环境处理。

## 请勿提交

- `project.private.config.json`
- 设备密钥、CloudBase 私钥或管理员会话
- OPENID、配对码、数据库导出与真实健康数据
- `.env`、日志、截图、构建缓存和本机运行证据

## V1 安全边界

- 小程序先通过账号身份读取授权药箱，不信任用户任意输入的设备 ID。
- 一次性配对码由 Station 管理端签发，云端仅保存 SHA-256 哈希并限制有效期与使用次数。
- 设备读取与写入均绑定授权药箱；页面切换药箱后拒绝旧请求回写。
- 小程序不直接监听受保护云集合。
- 小程序不提供远程开柜、批准或放行操作，并在 API 层拒绝 `OPEN_CABINET`。
- UI 过滤不是服务端授权替代品；生产环境必须同时部署匹配的 CloudBase membership 与 person scope 校验。

## 报告问题

请通过 GitHub Security Advisory 私下报告可能暴露账号、设备或健康数据的问题。报告中不要包含真实密钥、配对码或个人健康信息；使用脱敏复现数据即可。
