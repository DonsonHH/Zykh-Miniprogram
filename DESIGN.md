# 智药康护小程序设计基线

## 产品定位

小程序是**家属照护端**：负责看清今天要处理什么、提醒家人、查看终端回传的数据；Station 是现场端，负责人物信息、溯源码录药、AI 问询、测量与本地数据落库。移动端不复制大屏流程，也不伪造终端尚未回传的结果。

当前实体结构只有三个分类药盒，不存在独立电控药仓：

- `DAILY`：日常用药
- `SYMPTOM`：对症药品
- `CARE`：外用与护理

## 导航与页面职责

| Tab | 回答的问题 | 保留的主要操作 |
| --- | --- | --- |
| 首页 | 今天最该处理什么？ | 处理过期/临期/明确缺药，发送当日用药提醒，进入健康测量与近期详情 |
| 药库 | 药品放在哪一类盒中、状态如何？ | 按三个药盒浏览，搜索并查看效期、余量与溯源码 |
| 问询 | 家人近期得到什么健康建议？ | 按成员查看风险和问询摘要，不展示或伪造完整对话 |
| 照护 | 最近发生了什么？ | 在同一时间线筛选人工确认用药、体征与风险核验事实 |
| 家人 | 谁需要照护、终端是否可用？ | 查看成员当天待办、设备状态、低频协同与更换设备 |

## 信息层级

1. 首页只突出一项当前焦点；明确用药风险、已过期、临期、明确缺药和当日用药计划统一进入“优先处理”。
2. 不根据药品数量猜测缺药或低余量。只有终端同步 `inventoryState=DEPLETED` 才生成补药待办；`UNKNOWN` 只显示“余量待确认”。
3. 家人和设备不再与日常任务抢首屏：成员待办先于设备工具，设备工具先于最近命令日志。
4. “查看摘要”“近期详情”等文案必须与实际数据范围一致；不能把五条预览称为全部记录，也不能把摘要称为完整过程。

## 视觉语言

- 品牌使用 Station 同一枚透明图形标，旁边再显示“智药康护”文字；图标本身不嵌入重复文字。
- 普通列表与静态面板使用平面白色 surface；只有英雄任务、摘要和 Bottom Sheet 使用轻阴影的 raised surface。
- 与 Station 统一：底色 `#F6F9FC`、主操作蓝 `#2563EB`、正常/在线青绿 `#0F9F8F`、问询紫 `#7C3AED`、提醒橙、风险红。
- 每页最多一个强主操作；主分区是轻边界、轻阴影的白色 surface，列表行不再各自漂浮成卡片。
- 状态必须同时有文字和颜色：红=明确风险/过期/失败，橙=待处理/待复核，绿=正常/在线，蓝=可操作。
- 所有页面复用 `appHeader`、`care-ui.wxss` 的状态、列表和 Bottom Sheet 基础样式，避免页面自行复制品牌头和弹层。

## 关键数据与同步规则

- 有效期保留包装精度：`YYYY-MM` 按当月最后一天计算，`YYYY-MM-DD` 按具体日期计算；两种别名冲突时拒绝提交。
- 药品由 Station 扫描溯源码入库并上传；小程序不创建仓位，也不发出开仓或出药命令。
- 每条药品使用稳定 `medicineId` 作为身份，并同步 `storageBox=DAILY|SYMPTOM|CARE`。`slot` 仅为旧数据迁移字段。
- 库存事实只接受 `STOCKED / DEPLETED / UNKNOWN`。数量可以展示，但不能单独推导缺药、低余量或补药待办。
- 现场取药后由终端写入人工确认事实，如 `usage_status=TAKEN`；小程序不得把命令完成或风险核验通过冒充为已经用药。
- 远程用药提醒只对可执行的当日计划创建命令，并同时携带 `target_user_id` 与 `service_user_id`，保证与 Station 家属身份一致。
- 家属问询页只消费已同步的摘要、风险和建议；完整对话仍属于详情数据，不能由摘要拼接冒充。

## 交互安全

- 将精确日期切换为“按月份”前必须确认，因为会放弃具体日期。
- “重置未保存修改”恢复进入页面时的已同步内容，不清空远端药品。
- 切换设备后重建实时订阅；设备状态、记录与成员信息以 Station 回传为准。
