# 变更记录

本项目从 `v1.0.0` 开始使用语义化版本号。

## [Unreleased] - 2026-08-22

### Release A 同步准备

- 云端升级为 `3.0-three-box-library` Release A，只声明心跳、既有授权和板端药品快照七项能力。
- 生产板端鉴权改为逐设备 `DEVICE_SECRETS`，不再接受共享密钥回退。
- 新增带 lease、fencing、JCS-SHA256 和不可变版本行的药品快照协议。
- `REPORT_DEVICE` 使用服务端时间和安全字段白名单，不接收人物、问询或体征摘要。
- 人物数据、自助配对和远程命令在迁移完成前失败关闭。

### 小程序

- 连接状态拆分为加载、在线、心跳过期、暂不可用、未授权和版本不兼容。
- 药库以板端 finalized manifest 为唯一可见集合，本地参考表只补充展示资料。
- 药品基线更新为 23 种、三柜 `9 / 8 / 6`，保留 S09 并修正 S03/S13 稳定身份映射。
- 移除生产端远程药品写入入口，旧维护页面仅提示到 Station 现场操作。

> 本节描述仓库中的待发布实现，不代表线上云函数或真实 Station 已部署。

## [v1.0.0] - 2026-08-19

V1 系列最终版。

### 新增

- 家庭照护首页、23 仓药箱、药品维护、问询、照护记录、健康测量、家人与个人详情完整流程。
- 账号授权药箱列表与一次性配对码兑换。
- 用药安全事件的列表、分页、详情与家属已读回执。
- 人物稳定 ID、`personaGeneration` 与归档 tombstone 隔离。
- 体征 `MEMBER / BROKEN_INQUIRY / STANDALONE / LEGACY` 归属模型。
- `STOCKED / DEPLETED / UNKNOWN` 显式库存事实。
- CloudBase `2.7-runtime-consistency` 能力协商。

### 优化

- 五个主 Tab 统一为家庭照护视觉语言与单焦点 CareScreen。
- 删除重复大按钮，导航或详情操作改为整卡、整行点击。
- 首次加载、错误、陈旧数据、旧云不支持和无权限状态使用诚实文案。
- 问询列表保持摘要化，过程详情按需加载并明确最近 60 条同步窗口。
- 安全事件支持 100 条以上游标分页、去重、稳定排序和重复游标失败关闭。

### 安全与一致性

- 所有设备请求固定发起时的 `deviceId`，切换药箱后丢弃迟到响应。
- 受保护数据只经作用域 API 读取，不直接监听云数据库集合。
- 小程序端拒绝 `OPEN_CABINET`，安全事件不创建执行命令。
- 取药事实只接受明确的物理成功证据；阻断或未知结果不会显示为“已取药”。
- 冲突的事件、库存、体征或人物身份字段均失败关闭。

### 验证

- 428 项 Node.js 自动化测试通过。
- 122 个小程序文件通过 UI 静态校验。

### 已知边界

- 板端离线时，小程序只能显示最后同步事实与最后在线时间，不能代替 Station 恢复心跳。
- 配对码由 Station 管理端临时生成，明文只显示一次；仓库和云端均不保存可反查的原码。
- V1 的数据契约依赖匹配的 CloudBase 与 Station 版本；旧云能力缺失时会明确降级。
